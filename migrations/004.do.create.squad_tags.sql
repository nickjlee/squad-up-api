CREATE TABLE tags(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    tag TEXT NOT NULL
);

CREATE TABLE squads_tags(
    squad_id INTEGER REFERENCES squads(id) NOT NULL,
    tags_id INTEGER REFERENCES tags(id) NOT NULl
);